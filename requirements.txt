# MaskON development dependencies.
# Install with: pip install -r requirements.txt
#
# The service core (detectors, validators) only uses the Python standard
# library — no runtime dependencies yet. FastAPI/Pydantic will be added here
# when we build the API.

pytest>=9
ruff>=0.15
mypy>=1.10
