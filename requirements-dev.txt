# MaskON development dependencies — tests, lint, types.
# Install with: pip install -r requirements-dev.txt
-r requirements.txt
pytest>=9
ruff>=0.15
mypy>=1.10
httpx>=0.27  # used by FastAPI's TestClient
